- name: Add lxcif0 interface to container.
  copy: src=lxcif0 dest=/etc/network/interfaces.d/
  register: lxcif0_result

- name: Bring up lxcif0.
  command: bash -c "ifup lxcif0 || sleep 30 && ifup lxcif0" # try twice to avoid timing issue on jessie
  when: lxcif0_result.changed
